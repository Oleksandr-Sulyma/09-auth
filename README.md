# NoteHub — Zustand & Next.js

Навчальний проєкт **NoteHub**, створений у межах завдання **08-zustand**. Застосунок дозволяє
переглядати, фільтрувати та створювати нотатки з підтримкою SSR/CSR, SEO та збереження чернеток
через Zustand.

---

## 🚀 Технології

- **Next.js (App Router)**
- **TypeScript**
- **Zustand** + `persist` (збереження draft у `localStorage`)
- **TanStack Query (React Query)**
- **Axios**
- **CSS Modules**
- **Prettier**

---

## 📁 Структура проєкту

- `app/` — маршрути та сторінки (App Router)
- `components/` — перевикористовувані компоненти (кожен у власній папці)
- `lib/api/` — модулі для роботи з API
- `lib/store/` — Zustand-стор (`noteStore.ts`)
- `types/` — спільні типи та інтерфейси (`note.ts`)

---

## 📝 Створення нотатки та draft

- Створення нотатки винесене на маршрут `/notes/action/create`
- Форма реалізована через стандартну HTML-форму з `form action`
- Усі зміни полів автоматично зберігаються у **draft** через Zustand
- Draft зберігається в `localStorage` і не втрачається після перезавантаження сторінки
- При успішному створенні нотатки draft очищається
- При натисканні **Cancel** draft зберігається

---

## 🔍 SEO

- Налаштовані `metadata` та `generateMetadata` для всіх сторінок
- Додані Open Graph теги (title, description, url, images)
- Підключено глобальний шрифт **Roboto** через `next/font/google`

---

## 🌐 Деплой

Проєкт розгорнуто на **Vercel**. При здачі роботи надаються:

- [посилання на GitHub-репозиторій](https://github.com/Oleksandr-Sulyma/08-zustand)
- [посилання на задеплоєну версію застосунку](https://08-zustand-eosin-two.vercel.app/)

---

## ✅ Статус

Проєкт відповідає всім вимогам завдання та готовий до перевірки 🚀
